import 'package:flutter/material.dart';

class PhoneCallScreen extends StatelessWidget {
  const PhoneCallScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Text('Phone call screen');
  }
}
